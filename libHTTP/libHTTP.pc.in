# libHTTP pkg-config file

prefix=@PREFIX@
exec_prefix=${prefix}
libdir=@LIBDIR@
includedir=${prefix}/include/libHTTP

Name: libHTTP
Description: libHTTP is a comprehensive HTTP connection and content retrieval library
Version: 0.0.1
Requires:
Conflicts:
Libs: -L${libdir} -lHTTP -Wl,-rpath,$(libdir}
Cflags: -I${includedir}
